{
  "name": "react-ssr",
  "version": "0.0.1",
  "description": "SSR Test",
  "main": "index.tsx",
  "scripts": {
    "build:server": "NODE_ENV=production node build-server.mjs",
    "build:client": "NODE_ENV=production node build-client.mjs",
    "build": "npm run build:client && npm run build:server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "patch-package"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild-plugin-polyfill-node": "^0.3.0",
    "patch-package": "^8.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "text-encoding-polyfill": "^0.6.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
